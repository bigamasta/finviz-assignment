/** Children fetched per page — shared by useChildren and QueryKeepers. */
export const CHILDREN_PAGE_SIZE = 100

/** Minimum query length before a search request is issued. */
export const SEARCH_MIN_LENGTH = 2
